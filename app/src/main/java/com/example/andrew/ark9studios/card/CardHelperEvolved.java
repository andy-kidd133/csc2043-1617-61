package com.example.andrew.ark9studios.card;

/**
 * Created by Emma on 24/04/2017.
 */

import android.content.Context;
import android.graphics.Bitmap;
import android.graphics.BitmapFactory;

import java.util.ArrayList;

/**
 * This class will store information for each instance variable of the evolved character card objects
 * */

/*public class CardHelperEvolved {




    private static Context context;

    public CardHelperEvolved(Context context){

        this.context = context;
    }


    private static String[] cardNames = {
            "PainterLvl2",
            "HistoryLvl2",
            "GeographyLvl2",
            "CompSciLvl2",
            "SoftwareEngineerLvl2",
            "ProductDesignerLvl2",
            "PhotographerLvl2",
            "ComputerGameDeveloperLvl2",
            "PoliticianLvl2"
    };

    private static int[] health = {5,5,4,6,8,5,4,5,4

    };


    private static int[] attackCost = {4,3,4,5,5,4,3,4,5

    };

    private static int[] attackDamage = {4,6,5,7,8,6,4,4,8

    };


    private static CardDepartment[] cardDepartments = {
            CardDepartment.ART,
            CardDepartment.HUMANITITES,
            CardDepartment.HUMANITITES,
            CardDepartment.COMPUTING,
            CardDepartment.COMPUTING,
            CardDepartment.ART,
            CardDepartment.ART,
            CardDepartment.COMPUTING,
            CardDepartment.HUMANITITES

    };

    private static Strength[] strength = {
            Strength.HUMANITIES,
            Strength.COMPUTING,
            Strength.COMPUTING,
            Strength.ART,
            Strength.ART,
            Strength.HUMANITIES,
            Strength.HUMANITIES,
            Strength.ART,
            Strength.COMPUTING,

    };

    private static Weakness[] weakness = {
            Weakness.COMPUTING,
            Weakness.ART,
            Weakness.ART,
            Weakness.HUMANITIES,
            Weakness.HUMANITIES,
            Weakness.COMPUTING,
            Weakness.COMPUTING,
            Weakness.HUMANITIES,
            Weakness.ART,

    };


    private static Bitmap painterLvl2= BitmapFactory.decodeResource(context.getResources(), R.drawable.card2);
    private static Bitmap historyLvl2= BitmapFactory.decodeResource(context.getResources(), R.drawable.card5);
    private static Bitmap geographyLvl2= BitmapFactory.decodeResource(context.getResources(), R.drawable.card7);
    private static Bitmap compSciLvl2= BitmapFactory.decodeResource(context.getResources(), R.drawable.card10);
    private static Bitmap softwareEngineerLvl2= BitmapFactory.decodeResource(context.getResources(), R.drawable.card12);
    private static Bitmap productDesignerLvl2= BitmapFactory.decodeResource(context.getResources(), R.drawable.card15);
    private static Bitmap photographerLvl2= BitmapFactory.decodeResource(context.getResources(), R.drawable.card24);
    private static Bitmap compGamesDevLvl2= BitmapFactory.decodeResource(context.getResources(), R.drawable.card26);
    private static Bitmap politicanLvl2= BitmapFactory.decodeResource(context.getResources(), R.drawable.card28);



    private static Bitmap[] cardImages = {painterLvl2,historyLvl2, geographyLvl2, compSciLvl2,
            softwareEngineerLvl2,productDesignerLvl2,photographerLvl2,compGamesDevLvl2,
            politicanLvl2

    };


    public static ArrayList<CharacterCardEvolved> generateCards(){

        ArrayList<CharacterCardEvolved> evolvedCharacterCards = new ArrayList<CharacterCardEvolved>();

        for(int i=0; i < cardNames.length; i++){
            evolvedCharacterCards.add(new CharacterCardEvolved(cardNames[i], health[i], attackCost[i], attackDamage[i],
                    cardDepartments[i], strength[i], weakness[i], cardImages[i]));
        }

        return evolvedCharacterCards;
    }


}*/

